/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['m.media-amazon.com', 'f.media-amazon.com', 'in.pinterest.com', "i.pinimg.com"],
    },
};

export default nextConfig;
